# @pie-lab/chat

Shared chat bridge types and utilities.

Source direction:

- Discord/Telegram bridge behavior comes from the existing `pi-chat` source at `https://github.com/jikime/pi-chat`.
- This package should stay SDK-neutral where possible.
- Provider-specific bot code and the web chat app belong in `apps/chat`.
